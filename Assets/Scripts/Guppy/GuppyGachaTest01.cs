using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class GuppyGachaTest01 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        DataManager.AddPoint(100000);
    }
}
